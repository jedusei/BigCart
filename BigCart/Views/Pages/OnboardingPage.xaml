<?xml version="1.0" encoding="utf-8" ?>
<pages:Page
    x:Class="BigCart.Views.OnboardingPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:BigCart.Views"
    xmlns:vm="clr-namespace:BigCart.ViewModels"
    vm:ViewModelLocator.AutoWireViewModel="True">
    <AbsoluteLayout>
        <CarouselView
            x:Name="carouselView"
            IndicatorView="{Reference indicatorView}"
            Loop="False"
            StyleClass="FillAbsoluteLayout">
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <AbsoluteLayout>
                        <Image
                            Aspect="AspectFill"
                            Source="{Binding Image}"
                            StyleClass="FillAbsoluteLayout" />
                        <FlexLayout
                            Padding="0,50,0,0"
                            AlignItems="Center"
                            Direction="Column"
                            StyleClass="FillAbsoluteLayout">
                            <Label
                                HorizontalTextAlignment="Center"
                                Style="{StaticResource TitleBold}"
                                Text="{Binding Title}"
                                WidthRequest="250" />
                            <Image
                                HeightRequest="50"
                                IsVisible="{Binding ShowLogo}"
                                Source="logo.png"
                                TranslationY="-5" />
                            <Label
                                Margin="0,17,0,0"
                                HorizontalTextAlignment="Center"
                                Style="{StaticResource TextSecondary}"
                                Text="{StaticResource LoremIpsum}"
                                WidthRequest="300" />
                        </FlexLayout>
                    </AbsoluteLayout>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>
        <StackLayout AbsoluteLayout.LayoutBounds="0,1,1,AutoSize" AbsoluteLayout.LayoutFlags="PositionProportional,WidthProportional">
            <IndicatorView
                x:Name="indicatorView"
                IndicatorColor="#DCDCDC"
                IndicatorSize="8"
                SelectedIndicatorColor="{StaticResource PrimaryColor}" />
            <Button Margin="20,14,20,30" Text="Get Started" />
        </StackLayout>
    </AbsoluteLayout>
</pages:Page>
